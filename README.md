# AstroBox NG Plugin Docs

基于 **Next.js + Fumadocs** 的 AstroBox 文档站。

## 本地启动

```bash
pnpm install
pnpm dev
```

打开 <http://localhost:3000>。

## 文档结构

- `content/docs/usage`：使用文档（当前为占位）
- `content/docs/plugin-dev`：插件开发文档（现有全部文档已迁移）

`plugin-dev` 与 `usage` 均为 Fumadocs root folder，可在侧边栏顶部切换。
